# VKF parallel handover — 2026-09-04

Status: **PAUSED at clean boundaries**. Old-host 30-minute heartbeat
`continue-vkf-browser-playground` is `PAUSED`. No agent should unpause it until
new computer owns work.

Repository: `https://github.com/svenviktorjonsson/vektor-flow.git`

Remote checkpoint branches listed below were pushed successfully. Handover file
lives on `codex/0.5/browser-readme-nbody`; after worktree creation, read it at
`.worktrees/handoff/pages/docs/HANDOVER-2026-09-04.md`.

## Read first

1. Read root `AGENTS.md`.
2. Read `CONTEXT.md`, `docs/agents/autonomous-delivery.md`,
   `docs/agents/language-design-authority.md`, and relevant ADRs.
3. User owns public VKF syntax, semantics, APIs, diagnostics, schemas, and ABIs.
   No public change without compact `ready-for-human` decision packet.
4. No fallbacks. Unsupported behavior must hard-fail with clear diagnostics.
5. Keep three lanes isolated and parallel. Root agent = integration steward.
6. Never use file counts as completion. Use acceptance gates below.

## Fast restart on new computer

```powershell
git clone https://github.com/svenviktorjonsson/vektor-flow.git
cd vektor-flow
git fetch origin --prune

git worktree add .worktrees/handoff/pages `
  -b codex/0.5/browser-readme-nbody `
  origin/codex/0.5/browser-readme-nbody

git worktree add .worktrees/handoff/bootstrap `
  -b codex/0.5/050-i240-stage3-pe-crt `
  origin/codex/0.5/050-i240-stage3-pe-crt

git worktree add .worktrees/handoff/quantum `
  -b codex/quantum/qopt14-cooperative-cancellation `
  origin/codex/quantum/qopt14-cooperative-cancellation
```

If branches already exist locally, omit `-b` and use local branch names.

Spawn exactly three workers:

- Pages/README lane in `handoff/pages`
- 0.5 self-host lane in `handoff/bootstrap`
- proof-guided optimization lane in `handoff/quantum`

Root agent owns merge queue, final gates, push to deployment branch, GitHub
Pages verification. Do not mix lane edits.

## Global checkpoint

| Lane | Verified status | Clean restart branch | Pre-handover HEAD |
| --- | ---: | --- | --- |
| README/Pages | 24/26 existing fences | `codex/0.5/browser-readme-nbody` | `a75e70484690560f88766e34eeb39b507bfb80c0` |
| Fully self-hosted 0.5 | 60% conservative (`5/8` weighted ADR-0005) | `codex/0.5/050-i240-stage3-pe-crt` | `cc310f23ccf1f5dd836ffe1f33218501d4ef83a0` |
| Proof-guided optimization | 99%, never claim 100 yet | `codex/quantum/qopt14-cooperative-cancellation` | `23e52cbeaebede1e02871a4f4d8ea4365c776f11` |

All three worktrees were clean. Current packets stopped before RED. No partial
implementation needs recovery.

## Lane A — README-first client-only Pages

### User-visible contract

- Site root = repository README as technical reference. No marketing/catalog
  shell.
- Every VKF fence directly editable inline.
- `Play` executes edited code in place. It must not open another editor/page.
- Console appears below code after run.
- Visual `Result` pane appears to code's right only when compiled program emits
  validated UI/visual output.
- VKF-owned syntax highlighting only inside editor. No Prism/CDN.
- Client-only execution. No server compilation or execution.
- User code gets no network, filesystem, process, DOM, localhost, or host API.
- Fresh Web Worker per run; bounded timeout; terminate worker after run.
- Browser compiler WASM imports must be exactly `[]`.
- No fallback renderer/evaluator. Unsupported code hard-fails.
- Benchmark copy: published timings belong to 0.3.0; 0.4.1 adds UI/retained
  rendering without relabeling old evidence; 0.5.0 reruns full matrix.
- Domain: `vektorflow.org`. Pages workflow triggers `main` and
  `codex/0.5/integration`; do not push integration until release-ready.

### Current branch and proof

Worktree on old host:
`.worktrees/0.5/browser-readme-spectral-norm`

Branch: `codex/0.5/browser-readme-nbody`

HEAD before handover commit:
`a75e70484690560f88766e34eeb39b507bfb80c0`

Status: clean. README26 work not started.

Verified coverage: **24/26** existing README VKF fences. Runnable:
README01–10 and README12–25. Missing:

- README11: HTML controls/events
- README26: N-body

Latest focused coverage gate:

```powershell
node --test tests/js/pages-readme-browser-coverage.test.mjs
```

Result: `22/22`, 11.6 s. Latest full Pages/browser gate: `71/71`. Fresh
browser-WASM, Stage2 fixed point, source graph, and imports `[]` passed at
Fannkuch checkpoint.

### Restart packet A1 — README26 N-body

Canonical source: README around lines 1040–1121. Recorded expected output:
`-0.16907807065935543`.

Required general tracer/runtime:

- parse typed `System` record and destructuring
- derive fixed shape `5 x 3` from source
- parse/copy source positions, velocities, masses
- derive solar mass, days/year, timestep, loop steps
- validate structural offset/advance/energy loops
- support nested fixed-vector arithmetic/lifting, norm/power, pairwise ranges,
  record rebinding/return, energy reductions
- bound `steps <= 100000`
- flatten validated plan into raw `f64` arrays
- use general name-independent bounded pairwise vector-system opcode
- no `n_body` name match, canned result, JS evaluator, server, or fallback

Main blocker: exact numeric reconciliation. Current native VKF executable gives
`-0.16907807065934341`; ordinary C/JS/source-order gives about
`-0.16907807065934344`; literal `pow(sqrt(sumSquares), 3)` gives
`-0.16907807065935646`. None equals README record. Do not accept tolerance.
Find established VKF evaluation/reduction tree that yields recorded output, or
send user a decision packet if README evidence is stale.

TDD order:

1. exact canonical RED
2. source mutations for timestep/steps/masses/body coordinates/final energy
3. structure/budget hard errors
4. exact numeric tree proof
5. focused, full, graph, fresh-WASM, fixed-point gates
6. clean commit

### Restart packet A2 — README11 controls

Blocked on real resumable browser-WASM owner-event ABI. Do not simulate events
in JS or invent DOM/control semantics. Need compiled code continuation/update
path, exact HTML asset/control validation, deterministic event ordering, clear
errors, fresh Worker lifecycle. Produce `ready-for-human` packet if public ABI
surface must change.

### Approved lowercase `n` / `f` direction

User decisions already made:

- lowercase `n` = view distribution/matching key
- distinct view-key set across add calls behaves as union
- matching `n` values compose all matching add contributions as layers in one
  view; data itself is not deduplicated
- example required in README: add A with `n:[1,2,3]`, add B with
  `n:[3,4,5]` -> five toggleable views; `n=3` visibly contains both layers
- lowercase `f` = frame distribution/matching key
- `f_mode` decides how frames stack/present
- uppercase `N` object/system-member axis and uppercase `F` face axis remain
  unrelated

Still requires user decision before public implementation:

- stable-first-seen vs sorted key order
- behavior when an add omits `n` or `f`
- duplicate key behavior within one add
- key domain: integers, finite numbers, text, other comparable scalars
- exact `f_mode` vocabulary and default

Recommended packet previously sent but unanswered: stable first occurrence;
missing key broadcasts inside container; duplicate entries retained in same
view/frame; comparable scalar keys; same matching rules for `f`; `f_mode`
presentation only. Ask user; do not assume.

Current compiler whitelist/typed UI IR/retained packets contain no `n`, `f`, or
`f_mode`. `layer_axes` is temporal `t` only; do not reuse it.

### Pages linear commit chain

```text
f91cb6d0fcd902f604367a1a5eb1ebd838105435  inline README runner
77719bf0194a3374ed53377e0729f6852fac851e  vector lifting
bf1d69ea929a24ea2893d264f6ca09dcd7458c03  named axes
cad496d69d43892dc51086310e0fbf5a6c1478c7  retained display
d576bed9a269412def0f8d1d7104b70461bb771f  batched display
c32c331be2d4b8f021e1aa9bb95c6d2eb19362f7  camera-lit surfaces
820954b38ff8da25abf772959faea2b860b3306e  materials/shadows
6b89bbc24c54f767561bcfe9cdd97fdb0bca542c  textures
a27d9e083754baa874f748379cfc2c5d078e8a6b  transparency
4d83c414519a69c59fa15b6515302783be535f76  mirrors
e46ef4094da3572e808fb3f9b7bb4b2bc654b78c  World embedding
99b402d882a9ffe8adef13057219c62c385c1522  indexed field meshes
07be78b20ff5f04c537337489cceb6980798dc78  roughness cubes
0b50ec353a303887eabc8bb66534f6348cade4b9  spotlights
f4bd6939be404c4bbc5da43f99f853e3b9f70f27  procedural dice
b546f23dd3aab4d26e8a2a68003b285ca8987b53  layered glass
5238b4af941aab3cfea01e670cfde72af6e80e46  sun reflection
08c5c581dd15232e66a194ec5e41857ee615a863  rigid body
2cc895bde708da91bedf890531c04e1940a71f0e  Basic Syntax
ef5a94477ad2620cbf8023cf23dbd81277786c7f  spectral norm
a75e70484690560f88766e34eeb39b507bfb80c0  Fannkuch
```

### Final Pages gates

After README26, controls, and approved `n`/`f` example:

1. focused coverage test
2. all eight Pages/browser JS suites
3. bootstrap source-graph tests
4. fresh browser compiler/WASM build
5. inspect imports: exactly `[]`
6. Stage2 locked-source fixed point
7. `git diff --check`
8. reconcile old README 0.4.0 RC wording near lines 18/713 with required
   0.4.1 wording; keep 0.3 benchmark attribution near lines 829–832
9. single integration queue review
10. push `codex/0.5/integration`, wait Pages workflow, verify public
    `https://vektorflow.org`
11. verify TLS certificate; enable HTTPS enforcement only when available

## Lane B — 0.5 fully self-hosted compiler

Worktree on old host:
`.worktrees/0.5/050-i240-stage3-pe-crt`

Branch: `codex/0.5/050-i240-stage3-pe-crt`

HEAD: `cc310f23ccf1f5dd836ffe1f33218501d4ef83a0`

Status: clean. I240 stopped before RED. Only ignored standard binaries existed
under `.work/full-suite-bin`.

ADR-0005 weighted status: `5/8 = 62.5%`; report conservatively as **60%**.
Rules 1, 2, 7 complete; 3, 4, 5, 8 partial; 6 open.

### Restart packet B1 — I240 own `.CRT`

Next smallest active opaque body:

- `.CRT` virtual size 8, raw size 512
- first bytes `18 11 00 40 01 00 00 00`
- little-endian VA `0x0000000140001118`
- remaining 504 bytes zero
- I239 already owns required DIR64 relocation: pre-insertion page RVA `0x5000`,
  offset `0`

RED:

- modify existing public seed fixture to omit `.CRT` and `.reloc`
- caller supplies 2,560 opaque bytes: 2,048 pre-CRT + 512 `.rsrc`
- current `runner_seed` must reject expected 3,072 bytes

GREEN:

- private seed input becomes 2,560
- emit source-owned `_u64_bytes(5368713496)` + 504 zeros between sliced
  prefix/resource
- retain source-owned `.reloc`
- expected paths: `compiler/self_hosted/pe_x64.vkf`, bootstrap manifest,
  existing code-section fixed-point test, new evidence
- `compiler.vkf` likely unchanged

Required gates:

- focused seed
- PE/container 6-case suite
- source graph 2/2
- locked S2/S3/S4 fixed point
- unchanged 60-second full bundle twice
- serial adjacent 19/19
- direct PE compile must report `artifact_fallback:false`

Last I239 proof:

- focused 1/1, 21.35 s
- PE 6/6, 135.40 s
- source 2/2, 0.34 s
- locked graph 1/1, 19.09 s
- full bundle 1/1 at 26.14 s and 27.30 s
- adjacent 19/19, 379.03 s
- exact 5,120-byte PE prints `42`
- S2=S3 program; S2=S3=S4 compiler

Prerequisite `1e891a7b`/original `43d3c3e4` removed duplicated bundle frontend:
same invocation's emitted typed IR feeds linked x64 backend. Never restore
external lexer/parser/IR duplication. Gate went from timeout 61.12 s to
49.18/41.23 s, then 26–27 s integrated. No cache or timeout change.

Strict rules: direct-only; no `process.run_native`; no fallback; no timeout or
assertion relaxation; preserve invalid-UTF8 opaque bytes exactly; keep public
syntax/API/schema/ABI unchanged.

Remaining after `.CRT`: own import/rdata/pdata/resource bodies, then other
platform seeds/containers, broad strict ecosystem gate, fallback removal.

### Bootstrap descendant chain

```text
f9f8d658 signed writer
df913c16 signed division
a8080db3 floating negation
6c126c3a signed mixed addition
cdbdd81a signed mixed multiply
2dc09ba5 signed mixed subtraction
b7fda86c signed mixed division
96797a11 forward call relocation
f12f9ba9 backward call relocation
d001c53f source symbol positions
7b2f3b0a relocation collection
d8dddb73 dynamic relocations
b143e831 x64 section padding
59a94173 marker discovery
9b8836f0 byte-preserving section replacement
46d66055 PE section discovery
60a97f1d missing section creation
6f44da43 packed PE growth
e9496ac2 middle insertion
202228b1 resource relocation
8c9b1ae2 PE seed ownership
1e891a7b direct typed-IR bundle prerequisite
cc310f23 base-relocation ownership
```

## Lane C — proof-guided “quantum” optimization

Worktree on old host:
`.worktrees/quantum/qopt14-cooperative-cancellation`

Branch: `codex/quantum/qopt14-cooperative-cancellation`

HEAD: `23e52cbeaebede1e02871a4f4d8ea4365c776f11`

Status: clean. QOPT14 stopped before RED; audit only.

Status: **99%**, never 100 until independent ADR-0010/T4 exclusive-runner
evidence and declared gates close.

Non-negotiable optimizer rules:

- optimization only after measured proof against baseline
- production miss = baseline + one guided candidate, never 256 runs
- exact/bit parity required
- only measured winner applies
- retained result keyed by exact function/program/dependency/host/toolchain/proof
  fingerprints
- source-irrelevant changes reuse proof; semantic dependency change remeasures
- explicit fail-closed rejection reasons
- atomic/durable deterministic concurrent cache
- source-ordered deterministic errors, mandatory cleanup
- thread failure aborts; no serial fallback

### Restart packet C1 — QOPT14 cooperative cancellation

Existing `automatic_static_loop_work` recognizes exact initialized counter,
fixed integer bound, unit-increment backedge. Existing x64 pair owns per-lane
result/error slots and always waits/closes, but lacks shared cancellation.

RED first:

- require explicit safe-backedge/cancellation receipt
- roots without proven safe polling point stay serial with explicit reason
- one root errors while sibling runs long loop
- simultaneous error keeps source-left exact error
- zero partial output; all handles/resources cleaned

GREEN:

- add one private shared cancellation flag to pair context
- exact root error sets flag
- sibling polls only proven loop backedges/safe points
- cancelled sibling exits without output/error
- always join/close
- propagate source-first exact error after join
- benchmark polling overhead against baseline + one guided candidate
- select only measured win
- retain all QOPT13 proof/fingerprint/invalidation behavior

QOPT13 latest proof:

- Clang O2 and MSVC Release: same seven focused tests green
- standalone emitter built
- exact fixed `SumF64Values` left-fold receipt only
- adversarial `1e16 + -1e16 + value` stays exact `42`, `43`
- unchanged source reuse = zero candidates
- operand-order mutation = exactly two candidates
- Clang 71.73 ms -> 38.42 ms
- MSVC 43.92 ms -> 25.21 ms
- no portable speed claim

Remaining after QOPT14: independent T4; POSIX execution; wider result shapes;
dynamic/internal reductions; GPU scheduling; broader composition.

### QOPT chain

```text
edc0f42b retain measured decisions
99f0f5dd persist function proofs
0992b6c5 retained proof driver
459d4ecb compose function proofs
4a0fe0e6 gate dependencies
2109969d gate transitive effects
3ee34012 durable positive/negative proof cache
65c4dace scalar graph independence
9a20e8e4 measured threaded pair
b6d4d233 scalar parameter borrows
d129c21f root parameter provenance
11d77a1b deterministic threaded errors
23e52cbe exact source-order reductions
```

## Integration and remote safety

- Old main/root worktree had many unrelated user changes and untracked files.
  Never clean/reset it. Isolated lane worktrees are authoritative.
- Lane commits were not merged into `codex/0.5/integration` at pause.
- Integration branch local pre-handover HEAD:
  `f91cb6d0fcd902f604367a1a5eb1ebd838105435`.
- Use one integration steward. Review commit ancestry, contract hashes, RED/GREEN
  receipts, generated artifacts before merge/cherry-pick.
- Do not force-push. Do not push deployment branch until all required Pages
  behavior and final gates pass.
- Resume 30-minute progress automation only on owning computer/thread. Report
  Pages, self-host, optimizer percentages from explicit gates.

## First message for successor agent

```text
Check out remote branch codex/0.5/browser-readme-nbody. Read
docs/HANDOVER-2026-09-04.md completely, then AGENTS.md and linked agent docs.
Start three isolated workers on listed remote branches. Do not change public VKF
contracts, invent f_mode values, accept N-body numeric tolerance, simulate
controls in JS, add fallback paths, relax timeouts, or touch unrelated root
changes. Continue README26, I240 .CRT ownership, and QOPT14 cooperative
cancellation in parallel using RED->GREEN receipts. Root owns integration.
```
