sy: .symbolic

test aliased_symbolic_module_preserves_nested_linalg_dependency() -> bit:
    x: R
    expanded: sy.expand((x + 1)^3)
    (sy.term_count(expanded) = 4 /\ sy.evaluate(expanded, [2]) = 27)?!
