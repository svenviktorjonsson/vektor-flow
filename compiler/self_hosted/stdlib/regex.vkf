# Native compiler-specialized regular expressions. Patterns are compile-time
# constants; source text remains runtime data.

regex_stdlib_capability:
    name: "regex"
    pattern_policy: "compile-time regular expression"
    direct: ["match", "groups"]
    :
