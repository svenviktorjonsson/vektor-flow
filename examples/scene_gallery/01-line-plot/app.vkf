: .ui.display
:.math

display: Display(dim:2)
frame: display.add_frame(pos:[0.08, 0.08], size:[0.84, 0.84])
x: [..512] / 256 - 1
y: 0.85 * sin(x * pi)
frame.add(
    x:x,
    y:y,
    id:"sine",
    color:[0.12, 0.72, 1.0, 1.0]
)
