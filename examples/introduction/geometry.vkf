: .ui.display
:.math

display: Display()
frame: display.add_frame(pos:[0.08, 0.08], size:[0.84, 0.84])
frame.add(
    p_u:num(
        [..512] / 256 - 1,
        0.85 * sin(([..512] / 256 - 1) * pi)
    ),
    id:"sine",
    color:[0.12, 0.72, 1.0, 1.0]
)
