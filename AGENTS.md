## Agent skills

### Issue tracker

Issues and PRDs are tracked in GitHub Issues for `svenviktorjonsson/vektor-flow`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the standard `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository with `CONTEXT.md` and architectural decisions under `docs/adr/`. See `docs/agents/domain.md`.
